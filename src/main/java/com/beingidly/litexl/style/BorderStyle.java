package com.beingidly.litexl.style;

/**
 * Border style options.
 */
public enum BorderStyle {
    /** No border. */
    NONE,
    /** Thin border. */
    THIN,
    /** Medium border. */
    MEDIUM,
    /** Thick border. */
    THICK,
    /** Double border. */
    DOUBLE,
    /** Dashed border. */
    DASHED,
    /** Dotted border. */
    DOTTED
}
