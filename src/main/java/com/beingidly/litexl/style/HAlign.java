package com.beingidly.litexl.style;

/**
 * Horizontal alignment options.
 */
public enum HAlign {
    /** General alignment (default). */
    GENERAL,
    /** Left alignment. */
    LEFT,
    /** Center alignment. */
    CENTER,
    /** Right alignment. */
    RIGHT,
    /** Fill alignment. */
    FILL,
    /** Justify alignment. */
    JUSTIFY
}
