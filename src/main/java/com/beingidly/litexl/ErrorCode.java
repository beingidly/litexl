package com.beingidly.litexl;

/**
 * Error codes for litexl exceptions.
 */
public enum ErrorCode {
    FILE_NOT_FOUND,
    FILE_CORRUPT,
    INVALID_PASSWORD,
    UNSUPPORTED_FORMAT,
    INVALID_ARGUMENT,
    IO_ERROR,
    XML_PARSE_ERROR,
    ZIP_ERROR,
    CRYPTO_ERROR,
    MAPPER_ERROR
}
