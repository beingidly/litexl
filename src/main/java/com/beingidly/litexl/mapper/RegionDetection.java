package com.beingidly.litexl.mapper;

/**
 * Region detection modes for sheet mapping.
 */
public enum RegionDetection {
    /** No automatic region detection. */
    NONE,
    /** Automatically detect data region by scanning headers. */
    AUTO
}
